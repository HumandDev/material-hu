import { BreadcrumbLink } from './types';
type Props = {
    links: BreadcrumbLink[];
    darkBackground?: boolean;
};
declare const BreadCrumbs: ({ links, darkBackground }: Props) => import("react/jsx-runtime").JSX.Element;
export default BreadCrumbs;
