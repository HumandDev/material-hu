import { RatingProps } from './Rating';
type Props = {
    name: string;
    inputProps?: RatingProps;
};
declare const FormRating: ({ name, inputProps }: Props) => import("react/jsx-runtime").JSX.Element;
export default FormRating;
